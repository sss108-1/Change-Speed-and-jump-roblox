# Change-Speed-and-jump
Change the speed with this simple Python script to be able to modify the speed value

To be able to run this you need the libraries that are in requirements.txt
To do this, run this command
pip -r requirements.txt

After that, just run the script
python ChangeSpeed.py
